

				TFS examples


	iorouter - example using the file system from a standard
		   toolset program.
	
	rtfs -     example of multiple client processes using the
		   remote TFS library to access the TFS server.
	
	tfs -	   examples of use of the local TFS library.

	tfsh - 	   example of customizing tfsh to provide special
		   facilities.


